//

function landpages() {
  return <div className="my-20"></div>;
}

export default landpages;

{
  /*
  
  <div className="mb-24">
        <h3 className="text-[#16BA16] mb-10 md:text-2xl text-center">
          Why{" "}
          <span className="bg-clip-text text-transparent bg-gradient-to-r from-[#16BA16] to-[#FFD700]">
            Choose
          </span>{" "}
          <span className="text-[#FFD700]">Us</span>
        </h3>
        <div className="md:grid  grid-cols-3 gap-6 w-11/12 mx-auto mb-7">
          {whyus.map((data, index) => (
            <div key={index} className="flex gap-5 md:block mb-9">
              <div className="bg-gradient-to-r from-[#16BA16] to-[#FFD700] p-[2px] rounded-full  w-11 h-11 md:w-fit mb-8 md:mx-auto">
                <div className="bg-white text-center text-[#16BA16] w-10 h-10 flex items-center justify-center rounded-full md:text-3xl">
                  {index + 1}{" "}
                </div>
              </div>
              <p className="lg:text-xl mb-12">{data.text}</p>
            </div>
          ))}
        </div>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 w-10/12 mx-auto gap-5 mb-24">
        {features.map((data, index) => (
          <div key={index}>
            <p>{data.icon}</p>
            <p className="text-base md:text-xl lg:text-[32px] mb-3">
              {data.text}
            </p>
          </div>
        ))}
      </div>

      <h3 className="text-center mb-16 text-[#16BA16] lg:text-2xl">
        Our{" "}
        <span className="bg-clip-text text-transparent bg-gradient-to-r from-[#16BA16] to-[#FFD700]">
          {" "}
          Network
        </span>
      </h3>
      {stepsData.map((steps, index) => (
        <div
          key={steps.header}
          className="grid grid-cols-1 md:grid-cols-2 w-11/12 mx-auto gap-10 lg:items-center md:gap-20 mb-20 md:mb-12 lg:mb-11 text-[#363537] px-4 md:px-0"
        >
          <div className={`${index % 2 === 0 && "md:order-2"}  md:w-10/12  `}>
            <img src={steps.images} alt={steps.images} className="" />
          </div>
          <div className={`${index % 2 === 0 && "md:order-1"}`}>
            <h3 className="text-xl lg:text-3xl mb-6 font-semibold ">
              {steps.header}
            </h3>
            <p className="leading-[1.6] lg:text-[18px] mb-5 ">{steps.text}</p>
            <button
              className="bg-gradient-to-r from-[#16BA16] to-[#FFD700] p-2  rounded-[48px] lg:rounded-[32px] text-white py-2 px-4"
              style={{ boxShadow: "2px 2px 4px 0px #00000026" }}
            >
              Get started
            </button>
          </div>
        </div>
      ))}
    </div>
  */
}
